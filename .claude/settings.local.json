{
  "permissions": {
    "allow": [
      "Bash(node scripts/refresh-game-files.js --check)",
      "Bash(echo \"exit=$?\")",
      "Bash(node scripts/refresh-game-files.js)",
      "Bash(node -e ' *)"
    ]
  }
}
